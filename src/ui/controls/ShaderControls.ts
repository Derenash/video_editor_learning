// ui/controls/ShaderControls.ts
class ShaderControls {
  private shader: BaseShader;
  private container: HTMLElement;

  constructor(shader: BaseShader) {
    this.shader = shader;
    this.createControls();
  }

  private createControls() { /* ... */ }
}